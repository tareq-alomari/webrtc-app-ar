// Create an instance of the SessionManager and initialize the application.
// The SessionManager will handle everything from there.
const sessionManager = new SessionManager();
sessionManager.init();